---
description: Markdown 文档规则
globs: **/*.md
alwaysApply: false
---

# Markdown 规则

- 文件命名使用 kebab-case 格式，例如 `markdown-rules.md`，排除少量专有文件名（如 `README.md`）
- 使用中文标点符号（全角标点）
- 标题层级清晰，最多使用 4 级标题
- 列表使用 `-` 或数字编号
- 代码块使用三个反引号包裹，指定语言
- 链接使用 `[文本](URL)` 格式
- 图片使用 `![描述](URL)` 格式
- 表格使用 `|` 分隔列
